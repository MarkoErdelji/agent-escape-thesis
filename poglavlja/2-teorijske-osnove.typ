= Теоријске основе
<теоријске-основе>

== Оперативни систем Linux

Linux је вишекориснички оперативни систем са подршком за више процеса, изграђен
око монолитног језгра које управља хардверским ресурсима (централна процесорска
јединица – CPU, меморија, складиштење и мрежни интерфејси). Он обезбеђује добро
дефинисан интерфејс програмима у корисничком простору (енгл. _user space_)
@love2010linux. Он је један од најраспрострањенијих оперативних система за 
серверску инфраструктуру и основа на којој су изграђене савремене контејнерске технологије
@sultan2019container.

Архитектура Linux-а намеће поделу између простора језгра (енгл. _kernel space_)
и корисничког простора. Језгро се извршава у привилегованом режиму централне
процесорске јединице (ring 0 на x86 архитектурама) са неограниченим приступом
хардверу и меморији. Програми у корисничком простору, насупрот томе, извршавају
се у ограниченом режиму и не могу директно манипулисати хардвером или
приступати меморији других процеса. Сва интеракција између корисничког простора
и језгра одвија се коришћењем системских позива (енгл. _system calls_ –
syscalls), контролисаног скупа улазних тачака кроз које програми захтевају
услуге попут улазно-излазних операција са датотекама (енгл. _input/output_ –
I/O), креирања процеса и мрежне комуникације @kerrisk2010linux. Ова раздвојеност
је примарна безбедносна граница у систему, што значи да грешка у апликацији
у корисничком простору не може, под нормалним околностима, компромитовати
језгро или друге процесе.

Процес је основна јединица извршавања којом језгро управља. Сваком процесу
додељује се јединствени идентификатор процеса (енгл. _process identifier_ –
PID). Он се извршава под одређеним идентификатором корисника (енгл. _user
identifier_ – UID) и одржава сопствени виртуелни адресни простор. Такође, поседује скуп
дескриптора датотека (енгл. _file descriptors_) за интеракцију са датотекама,
сокетима и другим I/O ресурсима. Процеси су организовани у хијерархијско стабло чији је корен PID 1, 
односно први процес у корисничком простору који језгро покреће током подизања система.

Традиционални Unix модел привилегија дели све кориснике у две категорије
@love2010linux. _Root_ корисник (UID 0) није подложан већини стандардних провера
дозвола у језгру и има готово неограничену контролу над системом. Сви остали
корисници подлежу дискреционој контроли приступа (енгл. _Discretionary Access
Control_ – DAC), где је приступ датотекама и ресурсима одређен власништвом и
битовима дозвола. Разлика између _root_ корисника и обичних корисника има
директан утицај на контејнеризацију, где процес који се извршава као _root_
корисник унутар контејнера може, али и не мора, поседовати привилегије нивоа
_root_ корисника на систему домаћина @lin2018measurement.

Контејнеризација се директно ослања на ове примитиве језгра. Именски простори
(енгл. _namespaces_) партиционишу видљивост процеса и ресурса, контролне групе
(енгл. _control groups_ – cgroups) намећу ограничења на потрошњу ресурса, док
привилегије (енгл. _capabilities_) разлажу монолитну _root_ привилегију на мање
јединице.

== Контејнеризација и Docker

Контејнеризација је лаки облик виртуализације на нивоу оперативног система у
коме језгро домаћина омогућава истовремено покретање више изолованих инстанци
корисничког простора које се називају контејнери. За разлику од традиционалних
виртуелних машина, које садрже комплетан оперативни систем госта изнад
хипервизора#footnote[Хипервизор (енгл. _hypervisor_) је софтверски слој
који омогућава покретање више виртуелних машина на истом хардверу.],
контејнери деле језгро домаћина и носе само извршне датотеке
апликација са својим библиотекама и конфигурацијама које су потребне у време извршавања
@merkel2014docker. Због овакве архитектуре, контејнери троше мање меморије, 
брже се покрећу и захтевају мање простора за складиштење. Самим тим, контејнери постају погодни за архитектуре 
засноване на микросервисима и испоруку у облаку @sultan2019container @felter2015updated. 
Међутим, пошто контејнери деле језгро домаћина, њихова изолација је слабија него код виртуелних машина.

=== Архитектура Docker-а

Docker, објављен као пројекат отвореног изворног кода (енгл. _open source_), 
популаризовао је контејнере тако што је обезбедио јединствен скуп
алата за изградњу, дистрибуцију и покретање контејнера @merkel2014docker.
Платформа Docker састоји се од неколико компоненти које међусобно сарађују
@bui2015analysis:

- *Docker демон (`dockerd`).* Позадински процес који пружа
  _Representational State Transfer_ (REST) API (енгл. _Application
  Programming Interface_) преко Unix сокета. Прихвата клијентске команде (нпр. `docker run`, `docker build`) и
  управља животним циклусом контејнерских слика, контејнера, мрежа и волумена.

- *`containerd`.* Извршно окружење контејнера које управља комплетним животним циклусом
  контејнера на домаћину. То обухвата пренос и складиштење слика, извршавање контејнера,
  надзор, као и складиштење и мрежно повезивање ниског нивоа. Docker демон
  делегира стварно извршавање контејнера компоненти containerd
  @sultan2019container.

- *`runc`.* Извршно окружење контејнера усклађено са стандардом иницијативе за
  отворене контејнере (енгл. _Open Container Initiative_ – OCI), које креира и
  покреће контејнере према OCI спецификацији извршног окружења (енгл. _OCI
  Runtime Specification_). containerd позива runc ради извршавања операција
  језгра ниског нивоа (постављање именских простора, примена контролних група,
  промена коренског система датотека), а runc се завршава чим се покрене
  иницијални процес контејнера. Пошто runc директно комуницира са механизмима
  језгра, рањивости у runc могу нарушити контејнерску изолацију.

- *Слике и слојеви.* Docker контејнерска слика је уређена колекција слојева система датотека, при
  чему сваки слој представља скуп промена у систему датотека произведених једном
  инструкцијом у Dockerfile-у. Слојеви су адресирани по садржају и деле се
  између слика и тиме се смањују захтеви за складиштењем. У време извршавања,
  слој за писање поставља се изнад слојева слике који су само за читање
  коришћењем обједињеног система датотека (енгл. _union filesystem_), чиме сваки
  контејнер добија привид променљивог коренског система датотека
  @combe2016docker.

=== Контејнери наспрам виртуелних машина

Разлика између контејнера и виртуелних машина је важна за разумевање 
безбедносног модела контејнера. Виртуелна машина покреће комплетно језгро госта
на емулираном или паравиртуелизованом хардверу којим управља хипервизор.
Хипервизор примењује изолацију на нивоу хардверске апстракције, што значи да
компромитовано језгро госта не може директно приступити меморији домаћина или
меморији других гостију без нарушавања изолације самог хипервизора
@sultan2019container.

Контејнер, насупрот томе, извршава се као група процеса на језгру домаћина.
Изолација се примењује механизмима језгра (именски простори, контролне групе,
привилегије и профили обавезне контроле приступа) уместо хардверским границама.
Рањивост у језгру домаћина, у извршном окружењу контејнера или погрешна
конфигурација која слаби изолацију на нивоу језгра може омогућити
контејнеризованом процесу приступ ресурсима домаћина. Sultan и сарадници
карактеришу ово као основни компромис где контејнери нуде супериорну густину и
перформансе, али је њихова изолација слабија од изолације хипервизора
@sultan2019container.

=== Животни циклус контејнера и OCI спецификација

OCI дефинише две спецификације @oci2024runtime: спецификацију слике (енгл.
_Image Specification_), која стандардизује формат слике, и спецификацију
извршног окружења (енгл. _Runtime Specification_), која стандардизује начин на
који извршно окружење контејнера креира и управља контејнерима. Спецификација
извршног окружења дефинише животни циклус контејнера кроз четири стања:
креирање, креиран, покренут и заустављен. Током креирања постављају
се именски простори, монтира коренски систем датотека и конфигуришу контролне групе
према конфигурационој датотеци `config.json` у JSON (енгл. _JavaScript
Object Notation_) формату.

Када корисник изда команду `docker run`, Docker демон креира контејнер
кроз containerd, који затим позива runc. Опције команде `docker run`
попут `--privileged`, `--cap-add`, `--security-opt` и `--pid=host` директно
утичу на примењене механизме изолације. Безбедносне импликације ових опција
разматрају се детаљно у поглављу @linux-isolation-mechanisms @martin2018docker
@combe2016docker.

== Механизми изолације у Linux-у
<linux-isolation-mechanisms>

Изолација контејнера заснива се на више механизама језгра који се користе заједно. 
Сваки вектор напада експлоатише слабљење или одсуство једног или више од ових механизама
@lin2018measurement @sultan2019container.

=== Именски простори

Linux именски простори деле глобалне ресурсе језгра тако да процеси
унутар именског простора виде изоловану инстанцу тог ресурса @sun2018security.
Језгро обезбеђује седам типова именских простора релевантних за
контејнеризацију:

- *PID именски простор.* Изолује простор идентификатора процеса. Први процес у новом PID именском
  простору добија PID 1 и не може видети процесе у родитељском именском
  простору. Процес у контејнеру не може директно да види процесе изван свог PID
  именског простора.

- *Именски простор монтирања.* Обезбеђује изоловани поглед на стабло монтирања система датотека. Именски
  простор монтирања контејнера типично садржи само слојеве слике обједињене у
  систем датотека и све експлицитно везане волумене. Ово спречава контејнер да
  види коренски систем датотека домаћина.

- *Мрежни именски простор.* Додељује контејнеру сопствени мрежни стек, укључујући интерфејсе, табеле
  рутирања, правила заштитног зида и опсег портова. Контејнер види само
  виртуелни Ethernet уређај повезан на мост на домаћину.

- *UTS именски простор.* Изолује име домаћина (енгл. _hostname_) и _Network Information Service_ (NIS)
  име домена, што омогућава сваком контејнеру да има сопствено име домаћина без
  утицаја на домаћина.

- *IPC именски простор.* Изолује објекте међупроцесне комуникације (енгл. _Inter-Process Communication_
  – IPC). У ове објекте спадају сегменти дељене меморије, редови порука и скупови семафора. Контејнери
  у различитим IPC именским просторима не могу приступити дељеној меморији
  других контејнера.

- *Кориснички именски простор.* Мапира идентификаторе корисника и група између контејнера и домаћина. Процес
  може имати UID 0 (_root_ корисник) унутар контејнера док се мапира на
  непривилеговани UID на домаћину. Овим се ограничава штета у случају нарушавања
  изолације контејнера.

- *cgroup именски простор.* Виртуализује поглед на `/proc/self/cgroup` тако да контејнер не може видети
  хијерархију контролних група домаћина и верује да је његов сопствени корен
  контролних група врх стабла.

Lin и сарадници показали су да је непотпуна изолација именских простора значајна
површина напада. Ово значи да ако се било који именски простор дели са домаћином (нпр. путем
`--pid=host`), модел изолације се знатно слаби @lin2018measurement.

=== Контролне групе (cgroups)

Контролне групе (енгл. _control groups_ – cgroups) ограничавају и евидентирају
потрошњу ресурса група процеса. Две верзије постоје у савременим Linux језгрима:

- *cgroups v1* користи хијерархију по ресурсу. Стабла за CPU, меморију, блок I/O
  операције, приступ уређајима и друге ресурсе су одвојена. У cgroups v1, контролер
  `devices` посебно је релевантан за безбедност контејнера јер ограничава којим
  чворовима уређаја (`/dev/sda`, `/dev/mem` итд.) контејнер може приступити.

- *cgroups v2* обједињује све контролере у јединствену хијерархију, поједностављује правила
  делегирања и побољшава конзистентност. Уводи концепт нитног подстабла (енгл.
  _threaded subtree_) и _Pressure Stall Information_ (PSI) интерфејс за надзор
  борбе за ресурсе.

У погледу безбедности, контролне групе имају две основне улоге. Прва улога је
спречавање контејнера да исцрпи ресурсе домаћина (CPU, меморију, I/O пропусни
опсег), што би представљало напад онемогућавања услуге (енгл.
_denial-of-service_ – DoS) против суседних контејнера. Друга улога је контрола приступа уређајима, 
где контролер уређаја служи као основни механизам контроле приступа. У
подразумеваној Docker конфигурацији, контејнерима је забрањен приступ већини
блок уређаја домаћина. Када се користи опција `--privileged`, ограничења
контролера уређаја се уклањају и тиме се контејнеру додељује приступ свим
уређајима домаћина. Ово је један од основних услова који омогућава нарушавање
изолације засновано на погрешној конфигурацији @sultan2019container.

=== Linux привилегије

Систем привилегија замењује раније описани бинарни модел скупом појединачних
привилегија @love2010linux. Свака привилегија управља специфичном класом
привилегованих операција. Неке привилегије релевантне за безбедност контејнера
укључују:

- `CAP_SYS_ADMIN`: Најшира привилегија. Она додељује дозволу за монтирање
  система датотека, конфигурисање именских простора, коришћење `ptrace`,
  позивање `bpf()` и многе друге привилеговане операције. Lin и сарадници
  описују је као изузетно широку привилегију која омогућава велики број
  операција критичних за изолацију @lin2018measurement.

- `CAP_SYS_PTRACE`: Дозвољава коришћење `ptrace()` за прикључивање на друге
  процесе. Ако контејнер поседује ову привилегију и дели
  PID именски простор са домаћином, може се прикључити на процесе домаћина и
  читати или мењати њихову меморију.

- `CAP_NET_RAW`: Омогућава креирање мрежних сокета. Ова привилегија омогућава
  креирање и слање мрежних пакета на нижем нивоу.

- `CAP_DAC_OVERRIDE`: Заобилази провере дозвола за читање, писање и извршавање
  датотека. Ово омогућава приступ датотекама без обзира на власништво и битове
  дозвола.

Подразумевани скуп привилегија Docker-а је намерно рестриктиван. Подразумевано,
контејнери добијају мали подскуп привилегија (укључујући `CAP_CHOWN`,
`CAP_SETUID`, `CAP_NET_BIND_SERVICE` и неколико других) док се привилегије попут
`CAP_SYS_ADMIN` одбацују. Опција `--cap-add` може селективно вратити
привилегије, а `--privileged` враћа све @sultan2019container.

=== Seccomp

seccomp (енгл. _Secure Computing Mode_) омогућава процесу да инсталира _Berkeley
Packet Filter_ (BPF). BPF je програм који пресреће сваки системски позив и одлучује да
ли да га дозволи, одбије или да заврши процес. Docker обезбеђује подразумевани
seccomp профил који блокира приближно 40 до 50 системских позива који се
сматрају опасним у контексту контејнерске изолације
@grattafiori2016understanding. Блокирани позиви укључују `mount`, `umount2`,
`ptrace`, `reboot`, `swapon`, `kexec_load` и друге који би могли омогућити
контејнеризованом процесу да манипулише стањем језгра домаћина.

Seccomp филтер је додатни слој одбране. Чак и ако контејнер поседује
`CAP_SYS_ADMIN`, не може успешно извршити `mount()` ако seccomp филтер блокира
тај системски позив. Када се користи `--privileged`, Docker у потпуности
онемогућава seccomp филтер @lin2018measurement.

=== Обавезна контрола приступа: AppArmor и SELinux

Docker подржава два оквира обавезне контроле приступа (енгл. _Mandatory Access
Control_ – MAC):

- *AppArmor*, који се подразумевано користи у Ubuntu-у, примењује профиле по програму који
  ограничавају приступ датотекама, употребу привилегија, мрежне операције и
  операције монтирања. Docker генерише (`docker-default`) AppArmor профил
  који, између осталих ограничења, спречава контејнере да
  пишу у `/proc/sysrq-trigger`, монтирају нове системе датотека и приступају
  `/sys/firmware` @grattafiori2016understanding.

- *SELinux*, широко коришћен у дистрибуцијама као што су Red Hat Enterprise Linux, CentOS
  и Fedora. SELinux додељује ознаке процесима и датотекама и примењује матрицу политика
  која ограничава начин на који означени субјекти могу приступити означеним
  објектима. Када је SELinux омогућен, процеси контејнера се извршавају са
  ограниченим типом (нпр. `container_t`) који спречава приступ датотекама
  означеним за домаћина @grattafiori2016understanding.

Оба MAC оквира обезбеђују заштиту независно од DAC дозвола и привилегија. Опција
`--privileged` значајно смањује примену подразумеваних безбедносних ограничења,
укључујући ограничења привилегија, seccomp-а и приступа уређајима. Тачно
понашање MAC механизама зависи од конфигурације система домаћина
@sultan2019container.

Сви описани механизми заједно чине основу изолације контејнера. Именски простори
партиционишу видљивост ресурса, контролне групе ограничавају потрошњу,
привилегије ограничавају операције, seccomp филтрира системске позиве, а MAC
оквири примењују додатне политике. Опција `--privileged` у Docker-у истовремено уклања
или значајно ублажава више ових ограничења. Привилеговани контејнер добија све привилегије, има уклоњен seccomp
филтер, поништен MAC профил и приступ свим уређајима домаћина. Након примене
`--privileged`, именски простори представљају један од главних преосталих
механизама изолације, али са свим привилегијама и неограниченим системским
позивима, нарушавање ових именских простора је значајно олакшано
@lin2018measurement @sultan2019container.

== Класификација рањивости контејнера

Нарушавање изолације контејнера означава ситуацију у
којој процес који се иницијално извршава у изолованом контејнерском окружењу
стекне могућност приступа ресурсима домаћина који му нису били доступни у оквиру
предвиђеног модела изолације @reeves2021container. До нарушавања изолације може
доћи услед погрешне конфигурације, рањивости извршног окружења или рањивости
језгра.

Reeves и сарадници анализирали су 59 CVE уноса за 11 извршних окружења
контејнера и идентификовали три основна извора рањивости: погрешне
конфигурације, рањивости извршног окружења контејнера и рањивости језгра
@reeves2021container. Ова подела је уједно основа за три експериментална сценарија у овом раду.
Сваки од три лабораторијска сценарија одговара једној категорији. Детаљна анализа налаза ове студије дата је у поглављу @преглед-литературе.

=== Рањивости погрешне конфигурације

Рањивости погрешне конфигурације не настају услед грешака у софтверу, већ услед
некоректних или превише пермисивних подешавања. У екосистему контејнера,
најчешће погрешне конфигурације укључују:

- *Опција `--privileged`.* Као што је описано у претходној секцији, ова опција значајно смањује
  подразумевана ограничења изолације. Упркос безбедносним импликацијама, понекад
  се користи у _Continuous Integration/Continuous Deployment_ (CI/CD) процесним
  токовима, _Docker-in-Docker_ поставкама и развојним окружењима ради погодности
  @combe2016docker.

- *Изложен Docker сокет.* Монтирање Unix сокета Docker демона (`/var/run/docker.sock`) у контејнер даје
  том контејнеру контролу над Docker демоном путем API-ја, укључујући могућност
  креирања нових привилегованих контејнера који монтирају коренски систем
  датотека домаћина @martin2018docker. У типичној конфигурацији, овај приступ је
  функционално еквивалентан _root_ приступу на домаћину. Овај образац често се
  среће у CI/CD процесним токовима где агент за изградњу (енгл. _build agent_)
  захтева приступ Docker-у ради изградње слика.

- *Прекомерне привилегије.* Додавање широких привилегија, попут `CAP_SYS_ADMIN`, без опције `--privileged`
  и даље знатно слаби изолацију. Ово слабљење потенцијално омогућава различите операције
  над системом датотека, рад са именским просторима и одређене нападе засноване
  на BPF-у.

- *Дељење именских простора домаћина.* Опције попут `--pid=host`, `--network=host` или `--ipc=host` директно деле
  одговарајући именски простор са домаћином, уклањајући границу изолације за ту
  класу ресурса.

Рањивости настале услед погрешне конфигурације су значајне јер не захтевају
експлоатацију грешке у софтверу. Граница изолације је ослабљена намерном одлуком
(или превидом), и нападач треба само да препозна лошу конфигурацију и користи
стандардне системске алате за нарушавање изолације @reeves2021container.

=== Рањивости извршног окружења контејнера

Рањивости извршног окружења контејнера су грешке у софтверу који креира
контејнере и управља њима, примарно у компонентама као што су runc, containerd и
CRI-O. Од ових компоненти, runc представља посебно критичну компоненту као 
извршно окружење ниског нивоа које директно манипулише примитивима језгра 
(именским просторима, контролним групама, коренским системом датотека) и 
извршава се са _root_ привилегијама на домаћину. Грешка у runc може омогућити 
контејнеру директан приступ ресурсима домаћина @reeves2021container.

Репрезентативан пример је *CVE-2024-21626* (неформално познат као _Leaky
Vessels_), рањивост у runc. Током креирања контејнера, runc отвара дескриптор
датотеке ка систему датотека домаћина. Услед грешке у управљању дескрипторима
датотека, не затвара дескриптор пре него што се покрене почетни (_entrypoint_) процес
контејнера. Иницијални процес контејнера (PID 1) наслеђује радни директоријум
који указује на путању изван коренског система датотека контејнера. Нападач који
детектује овај аномалан радни директоријум затим може пратити процурели дескриптор
датотеке ради читања и писања произвољних датотека на домаћину @snyk2024leaky.

Ове рањивости су обично сложеније за експлоатацију јер нападач мора да разуме како извршно окружење функционише и где се грешка налази.
Утицај може бити потпун приступ систему датотека домаћина @reeves2021container @snyk2024leaky.

=== Рањивости језгра

Рањивости језгра су најдубља класа нарушавања изолације контејнера, јер
контејнери деле језгро домаћина по дизајну @lin2018measurement. Грешка у језгру
која омогућава ескалацију привилегија или произвољан приступ меморији може се
експлоатисати из контејнера ради нарушавања изолације, без обзира на то колико
је коректно контејнер конфигурисан.

Истакнут пример је *CVE-2022-0847* (познат као _DirtyPipe_), рањивост у
подсистему `pipe` Linux језгра @kellermann2022dirtypipe. Грешка се налази у
начину на који језгро иницијализује структуре `pipe` бафера. Под одређеним
условима, непривилеговани процес може преписати податке у кешу страница,
укључујући странице које припадају датотекама само за читање. Могућност
експлоатације зависи од верзије језгра и специфичних услова приступа датотеци.
Пошто се кеш страница дели између свих процеса на систему (укључујући домаћина и
све контејнере), контејнеризовани процес на погођеној верзији језгра може
употребити DirtyPipe за модификовање садржаја извршне датотеке домаћина у кешу страница.  
Када домаћин накнадно изврши ту извршну датотеку, покреће се код нападача са привилегијама 
на нивоу домаћина.

DirtyPipe илуструје једну класу рањивости језгра, али површина напада је знатно
шира. Друге класе укључују грешке типа _use-after-free_ (приступ већ ослобођеној
меморији) у различитим подсистемима језгра, рањивости у eBPF подсистему, верзије језгра и
његове конфигурације, и грешке у интеракцији између безбедносних модула језгра
(енгл. _Linux Security Modules_ – LSM) и именских простора @lin2018measurement.
Заједничко овим класама је то што дељено језгро омогућава да се рањивости језгра
експлоатишу из контејнера, слично као из корисничког простора домаћина.

Рањивости језгра најтеже су за експлоатацију јер захтевају детаљно познавање
интерних механизама језгра и специфичне технике експлоатације. Такође могу имати
најтеже последице, пошто могу заобићи механизме изолације који се ослањају на
кориснички простор. Lin и сарадници констатовали су да је дељено језгро
фундаментална слабост модела контејнера и аргументовали да је ојачавање језгра
(енгл. _kernel hardening_) неопходно за јаку безбедност контејнера
@lin2018measurement.

== Велики језички модели
<large-language-models>

Велики језички модели су неуронске мреже засноване на архитектури трансформера,
које су претходно обучене на великим корпусима текста и, по потреби, додатно
обучаване за специфичне задатке. Додатно обучавање путем учења поткрепљењем из
људских повратних информација (енгл. _Reinforcement Learning from Human
Feedback_ – RLHF) подешава претходно обучени модел тако да боље прати
инструкције корисника, поред основне способности генерисања статистички
вероватних наставака текста @ouyang2022training.

=== Архитектура трансформера

Трансформер, који су увели Vaswani и сарадници, је архитектура неуронских
мрежа која се у потпуности ослања на механизме пажње (енгл. _attention_),
одустајући од рекурентних и конволуционих слојева коришћених у претходним
архитектурама @vaswani2017attention. Кључна иновација је механизам самопажње
(енгл. _self-attention_), овај механизам ради тако што за сваку позицију у улазној секвенци,
модел рачуна тежине пажње у односу на све остале позиције, чиме одређује колико информација
тече између њих. Вишеглава пажња (енгл. _multi-head attention_) омогућава моделу
да паралелно обраћа пажњу на различите аспекте улаза, на пример синтаксичке
односе у једној глави и семантичке у другој.

Комплетан слој трансформера састоји се од подслоја вишеглаве самопажње и мреже
директног простирања (енгл. _feed-forward network_), са резидуалним везама и
нормализацијом слоја. Савремени велики језички модели садрже од десетина до
преко сто таквих слојева. Модели коришћени у литератури о LLM агентима,
укључујући моделе испитиване у овом раду, су трансформери само са декодером
(енгл. _decoder-only_) @radford2018improving, који користе каузалну (маскирану)
самопажњу. Свака позиција може обратити пажњу само на себе и претходне
позиције, чиме се омогућава ауторегресивно генерисање текста токен по токен.

=== Претходно обучавање и скалирање

Савремени велики језички модели претходно се обучавају на корпусима који
обухватају стотине милијарди до билионе токена извучених са веб-а, књига,
репозиторијума кода и других извора текста. Циљ претходног обучавања типично је
предвиђање следећег токена, што значи да, за дати префикс токена, модел учи да предвиди
расподелу вероватноће преко следећег токена.Иако је циљ претходног обучавања једноставан, 
модели са довољно параметара и података могу да развију способности које нису директно задате 
током обучавања @brown2020language. Перформансе модела прате
предвидљиве законе скалирања (енгл. _scaling laws_) у функцији броја параметара,
величине скупа података и количине рачунарских ресурса @kaplan2020scaling.

Једна од способности која се појављује при довољном скалирању је учење из
контекста (енгл. _in-context learning_). Модел прилагођава своје понашање на
основу примера или инструкција обезбеђених у упиту у време
закључивања, без икаквог ажурирања градијената @brown2020language
@dong2024survey. Када је обезбеђен мали број примера улаз-излаз, ова поставка
назива се учење из мало примера (енгл. _few-shot learning_). Када се даје само
опис задатка без примера, то се назива учење без примера (енгл. _zero-shot
learning_). Обе способности релевантне су за сценарије LLM агената, где модел
мора одредити начин деловања на основу знања из претходног обучавања и
опсервација прикупљених током извршавања.

=== Резоновање ланчаним корацима

Wei и сарадници показали су да навођење великих језичких модела да производе
међукораке резоновања, техником названом ланчано резоновање (енгл.
_Chain-of-Thought Prompting_ – CoT). Ова техника значајно побољшава перформансе на задацима
који захтевају резоновање у више корака, као што су аритметички текстуални задаци
и закључивање здравим разумом @wei2022chain. Уместо директног пресликавања из
улаза у излаз, модел генерише секвенцу корака резоновања који чине имплицитно
решавање проблема експлицитним.

Ова техника је посебно корисна код задатака који захтевају више повезаних корака,
као што су дијагностика система и безбедносна анализа. У таквим задацима потребно 
је уочити проблем, поставити хипотезу и затим спровести одговарајуће кораке.

== LLM агенти и аутономно резоновање
<llm-agents>

LLM агент претвара језички модел из пасивног генератора текста у аутономни
систем који може прикупљати информације о свом окружењу, резоновати о
опсервацијама и предузимати акције ради постизања циља @wang2024survey. Yao и
сарадници формализовали су овај приступ кроз парадигму ReAct (енгл. _Reasoning
and Acting_), која испреплиће генерисање трагова резоновања са извршавањем
акција у окружењу @yao2022react.

=== Петља агента

Канонски LLM агент функционише у петљи која се смењује између три фазе
@yao2022react:

+ *Посматрање.* Агент прима опсервацију из окружења: излаз команде, садржај датотеке, порука о
  грешци или други структурирани податак.

+ *Резоновање.* Велики језички модел обрађује опсервацију у контексту свог циља, претходних
  опсервација и знања из претходног обучавања. Генерише траг резоновања на
  природном језику који одлучује о наредној акцији.

+ *Деловање.* Агент емитује структурирану акцију, типично позив алата, која модификује
  окружење. Резултат акције постаје наредна опсервација и петља се понавља.

Петља се наставља све док агент не утврди да је циљ постигнут, не наиђе на
неповратан неуспех или не исцрпи унапред дефинисан буџет (нпр. максималан број
итерација или потрошњу токена). Wang и сарадници прегледају простор дизајна LLM
агената и предлажу обједињени оквир од четири модула (профилисање, меморија,
планирање и акција), при чему модул планирања обухвата резоновање које разликује
агенте од једноставних система упит-одговор @wang2024survey.

=== Употреба алата и позивање функција

Код LLM агената важно је и на који начин модел задаје акције које треба извршити. Schick и сарадници показали су да језички модели могу научити да
самостално одлучују када и како да позивају екстерне алате, уграђујући позиве
алата у генерисани текст @schick2023toolformer. Савремени API-ји великих
језичких модела формализују овај концепт кроз позивање функција (такође названо
употреба алата). Моделу се обезбеђује шема доступних алата, где је сваки алат
описан именом, типовима параметара и описом на природном језику. Са овим информацијама 
модел може емитовати структуриран позив алата уместо (или уз) текстуални излаз. Типични
алати обухватају извршавање команди путем шела (енгл. _shell_), претрагу
датотека, интеракцију са API-јима или манипулацију базама података.

Механизам употребе алата обезбеђује јасно раздвајање између резоновања агента
(које обавља велики језички модел) и његових акција (које извршава оквир).
Велики језички модел никада директно не извршава команде. Он емитује
структуриране захтеве које оквир валидира и извршава, а затим враћа резултате као
наредну опсервацију у петљу агента.

=== Ограничења LLM агената

LLM агенти имају неколико ограничења која могу утицати на успешност експлоатације:

- *Контекстни прозор.* Сваки модел има ограничен контекстни прозор, односно максималан број токена
  који може обрадити у једном позиву. Историја конверзације расте са сваком
  итерацијом петље агента, и када достигне границу прозора, старије опсервације
  морају се сумирати или одбацити, што може довести до губитка критичних
  информација прикупљених у ранијим корацима @wang2024survey.

- *Халуцинације.* Велики језички модели могу генерисати текст који је синтаксички коректан и
  убедљив, али фактички нетачан. Овај феномен познат је као халуцинација (енгл.
  _hallucination_) @huang2025survey. У контексту безбедносне експлоатације, ово
  може значити генерисање наизглед валидних команди које заправо не постижу
  намеравани ефекат, или погрешно тумачење излаза алата.

- *Трошкови и буџет.* Закључивање великих језичких модела има трошкове сразмерне броју обрађених
  токена, при чему се улазни и излазни токени наплаћују различито. Дугачке
  епизоде експлоатације са многобројним позивима алата могу произвести велике
  трошкове. Из тог разлога, ограничења буџета (максималан број позива алата,
  максимална потрошња токена или временско ограничење) су стандардни елемент
  архитектуре LLM агената @wang2024survey.

Због ових ограничења, за успешну аутономну експлоатацију није довољно да модел препозна рањивост, 
већ мора и правилно да изведе потребне кораке у оквиру доступних ресурса.
